@PACKAGE_INIT@

INCLUDE(CMakeFindDependencyMacro)
# Add dependencies here if logger itself depends on other libraries that need to be found by consumers
# Example: find_dependency(Boost REQUIRED COMPONENTS system)
# find_dependency(cryptopp REQUIRED) # If cryptopp also exports a config

# Include the targets file
INCLUDE("${CMAKE_CURRENT_LIST_DIR}/logger-targets.cmake")

CHECK_REQUIRED_COMPONENTS(logger)
